# GNPy Simulator - OPTIONAL AI Agent add-on
# ---------------------------------------------------------------------------
# These dependencies are ONLY needed for the separate LLM/agent app
# (app_agent.py). The manual apps (app_2.py, app_CustomInputGenerator_v2.py)
# do NOT require any of these and keep working without them.
#
# Install with:   pip install -r requirements-agent.txt
# ---------------------------------------------------------------------------

# Agent harness (built on LangGraph / LangChain)
deepagents>=0.6.0
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# --- Local model clients (install at least one) ---
# Ollama (simplest local option): https://ollama.com
langchain-ollama>=0.2.0
# OpenAI-compatible endpoints (vLLM / llama.cpp server / LM Studio):
langchain-openai>=0.2.0
# In-process GGUF on GPU (llamacpp provider): ChatLlamaCpp lives here
langchain-community>=0.3.0

# llama.cpp GGUF backend (Windows-friendly, CUDA). For GPU offload install a
# CUDA-enabled wheel, e.g.:
#   pip install llama-cpp-python --extra-index-url \
#     https://abetlen.github.io/llama-cpp-python/whl/cu124
llama-cpp-python>=0.3.0
# Optional: download GGUF builds from Hugging Face (not needed if air-gapped)
huggingface_hub>=0.24.0

# --- Datasheet parsing (local, no network) ---
pdfplumber>=0.11.0   # text + table extraction from PDF datasheets
pypdf>=4.0.0         # lightweight PDF fallback
# openpyxl + pandas are already in the core requirements.txt
